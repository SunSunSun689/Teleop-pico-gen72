# 香橙派部署 Piper 双臂遥操记录

## 环境信息

- 设备：香橙派 AIPro 20T
- IP：192.168.110.49
- 系统：Ubuntu 22.04.3 LTS（aarch64）
- 内核：5.10.0+
- Python：3.11（miniconda）
- conda 环境：pico
- 项目路径：/home/HwHiAiUser/teleop_pico

---

## 问题一：gs_usb 驱动缺失

### 现象
USB-CAN 适配器（candleLight，1d50:606f）插入后 `lsusb` 能识别，但 `ip link show` 没有 `can0`/`can1` 接口。

### 根本原因
香橙派内核编译时 `CONFIG_CAN_GS_USB=not set`，`gs_usb` 驱动既没有内置也没有模块文件。

### 验证命令
```bash
# 确认硬件识别
lsusb | grep -i can

# 确认驱动缺失
modprobe gs_usb          # 报错：Module gs_usb not found
ip link show | grep can  # 无输出

# 确认内核配置
zcat /proc/config.gz | grep GS_USB
# 输出：# CONFIG_CAN_GS_USB is not set
```

### 解决方案：手动编译 gs_usb.ko

#### 1. 在本机下载 gs_usb.c 源码
```bash
curl -s "https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/plain/drivers/net/can/usb/gs_usb.c?h=v5.10.235" -o /tmp/gs_usb.c
```

#### 2. 传到香橙派
```bash
sshpass -p 'Mind@123' scp -o StrictHostKeyChecking=no /tmp/gs_usb.c root@192.168.110.49:/tmp/gs_usb.c
```

#### 3. 在香橙派上编译
```bash
mkdir -p /tmp/gs_usb_build
cp /tmp/gs_usb.c /tmp/gs_usb_build/

cat > /tmp/gs_usb_build/Makefile << 'EOF'
obj-m := gs_usb.o
KDIR := /usr/src/linux-headers-5.10.0+
all:
	make -C $(KDIR) M=$(PWD) modules
clean:
	make -C $(KDIR) M=$(PWD) clean
EOF

cd /tmp/gs_usb_build && make
```

#### 4. 安装并加载
```bash
mkdir -p /lib/modules/5.10.0+/kernel/drivers/net/can/usb/
cp /tmp/gs_usb_build/gs_usb.ko /lib/modules/5.10.0+/kernel/drivers/net/can/usb/
depmod -a
modprobe gs_usb
```

#### 5. 验证
```bash
ip link show | grep can
# 应看到 can0, can1
```

---

## 问题二：开机自动加载 gs_usb 和配置 CAN 接口

### 自动加载模块
```bash
echo 'gs_usb' > /etc/modules-load.d/gs_usb.conf
```

### 创建 systemd 服务自动配置 CAN 接口

> **注意：** `After=network.target` 会导致服务在 `gs_usb` 模块加载完成前执行，
> 出现 `RTNETLINK answers: Operation not permitted` 错误。
> 必须改为 `After=systemd-modules-load.service` 并加 3 秒延迟等待接口就绪。

```bash
cat > /etc/systemd/system/can-setup.service << 'EOF'
[Unit]
Description=Setup CAN interfaces
After=systemd-modules-load.service
Wants=systemd-modules-load.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStartPre=/bin/sleep 3
ExecStart=/bin/bash -c '\
    for iface in can0 can1; do \
        if ip link show $iface > /dev/null 2>&1; then \
            ip link set $iface type can bitrate 1000000 && \
            ip link set up $iface && \
            echo "$iface configured OK" || \
            echo "$iface config failed"; \
        else \
            echo "$iface not found, skipping"; \
        fi; \
    done'

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable can-setup.service
```

### 手动配置 CAN 接口（每次重启后或临时使用）
```bash
ip link set can0 type can bitrate 1000000
ip link set up can0
ip link set can1 type can bitrate 1000000
ip link set up can1
```

---

## 问题三：xrobotoolkit_sdk 编译（aarch64）

### 现象
项目依赖的 `xrobotoolkit_sdk` 是 pybind11 C++ 扩展，本机编译的是 x86_64 `.so`，不能在香橙派上使用。

### 解决方案

#### 1. 准备 aarch64 库文件
```bash
# 香橙派上已有系统安装的 aarch64 库
mkdir -p /home/HwHiAiUser/teleop_pico/dependencies/XRoboToolkit-PC-Service-Pybind/lib/aarch64
mkdir -p /home/HwHiAiUser/teleop_pico/dependencies/XRoboToolkit-PC-Service-Pybind/include/aarch64/nlohmann

cp /opt/apps/roboticsservice/SDK/arm64/libPXREARobotSDK.so \
   /home/HwHiAiUser/teleop_pico/dependencies/XRoboToolkit-PC-Service-Pybind/lib/aarch64/
cp /opt/apps/roboticsservice/SDK/include/PXREARobotSDK.h \
   /home/HwHiAiUser/teleop_pico/dependencies/XRoboToolkit-PC-Service-Pybind/include/aarch64/
# nlohmann/json.hpp 从本机 scp 传过来
```

#### 2. 编译安装
```bash
source /usr/local/miniconda3/etc/profile.d/conda.sh && conda activate pico
pip install 'pybind11[global]'
cd /home/HwHiAiUser/teleop_pico/dependencies/XRoboToolkit-PC-Service-Pybind
CMAKE_PREFIX_PATH=$(python -c 'import pybind11; print(pybind11.get_cmake_dir())') \
  python setup.py build_ext --inplace
pip install -e .
```

---

## 问题四：pyrealsense2 不支持 aarch64

### 现象
`pip install pyrealsense2` 无可用 wheel，且 `piper_teleop_controller.py` 顶层 import 导致模块加载失败。

### 解决方案
不使用相机，将顶层 import 改为懒加载：

```bash
# 注释掉顶层 import
sed -i 's/from xrobotoolkit_teleop.hardware.interface.realsense import RealSenseCameraInterface/# RealSenseCameraInterface imported lazily in _initialize_camera/' \
  /home/HwHiAiUser/teleop_pico/xrobotoolkit_teleop/hardware/piper_teleop_controller.py

# 在 _initialize_camera 方法内加懒加载
sed -i 's/                self.camera_interface = RealSenseCameraInterface(/                from xrobotoolkit_teleop.hardware.interface.realsense import RealSenseCameraInterface\n                self.camera_interface = RealSenseCameraInterface(/' \
  /home/HwHiAiUser/teleop_pico/xrobotoolkit_teleop/hardware/piper_teleop_controller.py
```

---

## 启动双臂遥操

```bash
# 确认 CAN 接口就绪
ip link show | grep can

# 激活环境并启动
conda activate pico
cd /home/HwHiAiUser/teleop_pico
python scripts/hardware/teleop_dual_piper_hardware.py

# 自定义 CAN 端口
python scripts/hardware/teleop_dual_piper_hardware.py \
    --right-can-port can0 --left-can-port can1
```

---

## 注意事项

- `can1` 出现 `NO-CARRIER` 说明第二个机械臂未上电或 CAN 线未接好
- 重启后 `gs_usb` 会自动加载（已配置 `/etc/modules-load.d/gs_usb.conf`）
- CAN 接口会由 `can-setup.service` 自动配置（bitrate 1000000）
- 项目中 `pyproject.toml` 已移除 `pyrealsense2`、`torch`、`ur_rtde`、`dex_retargeting` 等不需要的依赖

---

## 完整部署步骤（从零开始）

### 前提条件
- 香橙派已安装 miniconda（路径 `/usr/local/miniconda3`）
- 两个 candleLight USB-CAN 适配器
- 项目代码在本机 `/home/dora/teleop_pico/XRoboToolkit-Teleop-Sample-Python`

### 第一步：创建 conda 环境并安装依赖

```bash
# 在香橙派上
source /usr/local/miniconda3/etc/profile.d/conda.sh
conda create -n pico python=3.11 -y
conda activate pico

pip install placo==0.9.4 mujoco numpy meshcat opencv-python tyro rospkg piper-sdk \
    dynamixel-sdk 'pybind11[global]'
```

### 第二步：同步项目代码

```bash
# 在本机执行
sshpass -p 'Mind@123' rsync -av \
    --exclude='build' --exclude='__pycache__' --exclude='*.egg-info' --exclude='.git' \
    -e "ssh -o StrictHostKeyChecking=no" \
    /home/dora/teleop_pico/XRoboToolkit-Teleop-Sample-Python/ \
    root@192.168.110.49:/home/HwHiAiUser/teleop_pico/
```

### 第三步：编译 xrobotoolkit_sdk（aarch64）

```bash
# 在香橙派上
# 准备 aarch64 库目录
PYBIND_DIR=/home/HwHiAiUser/teleop_pico/dependencies/XRoboToolkit-PC-Service-Pybind
mkdir -p $PYBIND_DIR/lib/aarch64 $PYBIND_DIR/include/aarch64/nlohmann

# 从系统已安装的 SDK 复制库文件
cp /opt/apps/roboticsservice/SDK/arm64/libPXREARobotSDK.so $PYBIND_DIR/lib/aarch64/
cp /opt/apps/roboticsservice/SDK/include/PXREARobotSDK.h $PYBIND_DIR/include/aarch64/

# nlohmann/json.hpp 从本机传过来（本机执行）
sshpass -p 'Mind@123' scp -o StrictHostKeyChecking=no -r \
    /home/dora/teleop_pico/XRoboToolkit-Teleop-Sample-Python/dependencies/XRoboToolkit-PC-Service-Pybind/include/nlohmann \
    root@192.168.110.49:$PYBIND_DIR/include/aarch64/

# 编译（香橙派上执行）
conda activate pico
cd $PYBIND_DIR
CMAKE_PREFIX_PATH=$(python -c 'import pybind11; print(pybind11.get_cmake_dir())') \
    python setup.py build_ext --inplace
pip install -e .
```

### 第四步：修复 pyrealsense2 导入问题

```bash
# 在香橙派上执行（不使用相机）
sed -i 's/from xrobotoolkit_teleop.hardware.interface.realsense import RealSenseCameraInterface/# RealSenseCameraInterface imported lazily in _initialize_camera/' \
    /home/HwHiAiUser/teleop_pico/xrobotoolkit_teleop/hardware/piper_teleop_controller.py

sed -i 's/                self.camera_interface = RealSenseCameraInterface(/                from xrobotoolkit_teleop.hardware.interface.realsense import RealSenseCameraInterface\n                self.camera_interface = RealSenseCameraInterface(/' \
    /home/HwHiAiUser/teleop_pico/xrobotoolkit_teleop/hardware/piper_teleop_controller.py
```

### 第五步：安装项目

```bash
conda activate pico
cd /home/HwHiAiUser/teleop_pico
pip install -e . --no-deps
```

### 第六步：编译并安装 gs_usb 驱动

```bash
# 在本机下载源码
curl -s "https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/plain/drivers/net/can/usb/gs_usb.c?h=v5.10.235" \
    -o /tmp/gs_usb.c

# 传到香橙派
sshpass -p 'Mind@123' scp -o StrictHostKeyChecking=no /tmp/gs_usb.c root@192.168.110.49:/tmp/

# 在香橙派上编译
mkdir -p /tmp/gs_usb_build
cp /tmp/gs_usb.c /tmp/gs_usb_build/
cat > /tmp/gs_usb_build/Makefile << 'EOF'
obj-m := gs_usb.o
KDIR := /usr/src/linux-headers-5.10.0+
all:
	make -C $(KDIR) M=$(PWD) modules
clean:
	make -C $(KDIR) M=$(PWD) clean
EOF
cd /tmp/gs_usb_build && make

# 安装
mkdir -p /lib/modules/5.10.0+/kernel/drivers/net/can/usb/
cp /tmp/gs_usb_build/gs_usb.ko /lib/modules/5.10.0+/kernel/drivers/net/can/usb/
depmod -a

# 设置开机自动加载
echo 'gs_usb' > /etc/modules-load.d/gs_usb.conf
```

### 第七步：配置 CAN 接口开机自动启动

```bash
cat > /etc/systemd/system/can-setup.service << 'EOF'
[Unit]
Description=Setup CAN interfaces
After=systemd-modules-load.service
Wants=systemd-modules-load.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStartPre=/bin/sleep 3
ExecStart=/bin/bash -c '\
    for iface in can0 can1; do \
        if ip link show $iface > /dev/null 2>&1; then \
            ip link set $iface type can bitrate 1000000 && \
            ip link set up $iface && \
            echo "$iface configured OK" || \
            echo "$iface config failed"; \
        else \
            echo "$iface not found, skipping"; \
        fi; \
    done'

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable can-setup.service
```

### 第八步：验证部署

```bash
conda activate pico
cd /home/HwHiAiUser/teleop_pico

# 验证核心模块导入
python -c "
import xrobotoolkit_sdk; print('xrobotoolkit_sdk OK')
import xrobotoolkit_teleop; print('xrobotoolkit_teleop OK')
from xrobotoolkit_teleop.hardware.interface.piper import PiperInterface; print('PiperInterface OK')
from xrobotoolkit_teleop.hardware.dual_piper_teleop_controller import DualPiperTeleopController; print('DualPiperTeleopController OK')
"

# 验证 CAN 接口
ip link show | grep can

# 验证机械臂连接
python scripts/hardware/read_piper_joints.py --once
```

---

## 日常使用流程

```bash
# 1. 加载 CAN 驱动（重启后若 can-setup.service 未生效时手动执行）
modprobe gs_usb
ip link set can0 type can bitrate 1000000 && ip link set up can0
ip link set can1 type can bitrate 1000000 && ip link set up can1

# 2. 启动 XRoboToolkit PC Service
cd /opt/apps/roboticsservice && bash runService.sh

# 3. 设置 Home 位置（首次或调整后执行）
conda activate pico
cd /home/HwHiAiUser/teleop_pico
python scripts/hardware/read_piper_joints.py --set-home

# 4. 启动双臂遥操
python scripts/hardware/teleop_dual_piper_hardware.py
```
